# ============================================================
# InterviewVault Frontend — Environment Variables
# Copy this file to .env (Vite auto-loads .env in dev)
# ============================================================

# URL of your locally running backend (from backend/README: uvicorn on :8000)
VITE_API_URL=http://localhost:8000
